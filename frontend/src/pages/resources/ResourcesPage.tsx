import { ResourceCataloguePage } from './ResourceCataloguePage';

/**
 * ResourcesPage — Successfully integrated with Module A (Facilities & Assets Catalogue)
 */

export default function ResourcesPage() {
  return <ResourceCataloguePage />;
}
